info:
  name: User login using creds
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "thisissarthak@gmail.com",
        "password": "ogsarthakcoder"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
